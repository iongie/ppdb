import { SidebarDropdownDirective } from './sidebar-dropdown.directive';

describe('SidebarDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
